fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.8.2
pandas==2.2.2
pyarrow==18.1.0
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.1
# 1.x pulls curl_cffi (browser TLS impersonation) — required for the Yahoo options
# endpoint from datacenter/AWS IPs, which 401s plain-requests (lstm_quant options flow).
yfinance==1.2.0
# Optional: real-greeks options source for lstm_quant (QUANT_OPTIONS_SOURCE=schwab).
# Needs an OAuth token (scripts/schwab_auth.py); falls back to yfinance if absent.
schwab-py>=1.0.0
joblib==1.4.2
PyYAML==6.0.2
requests==2.32.3
beautifulsoup4==4.12.3
psycopg2-binary==2.9.9
python-dotenv==1.0.1
asyncpg==0.29.0

# Deep learning
# TensorFlow is required for .keras LSTM artifacts.
# Torch is optional and only needed for legacy .pt artifacts/training.
tensorflow>=2.10.0
torch==2.8.0

# Technical indicators (fallback in code, not required)
# pandas_ta>=0.3.14b0

# Authentication
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
python-multipart>=0.0.6
